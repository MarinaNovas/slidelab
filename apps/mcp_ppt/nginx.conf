server {
    listen 80;
    server_name olega-mcp.ru;

    location /mcp {
        add_header WWW-Authenticate 'Bearer realm="mcp"' always;
        return 401;
        proxy_pass http://mcp:8001/mcp;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto http;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name olega-mcp.ru;

    ssl_certificate /etc/letsencrypt/live/olega-mcp.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/olega-mcp.ru/privkey.pem;

    location /exports/ {
        proxy_pass http://api:8000/exports/;
    }

    location /mcp {
        add_header WWW-Authenticate 'Bearer realm="mcp"' always;
        return 401;
        proxy_pass http://mcp:8001/mcp;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location / {
        proxy_pass http://api:8000/;
    }
}