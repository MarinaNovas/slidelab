log_format debug_headers '$remote_addr - $host "$request" '
                         'auth="$http_authorization" '
                         'x_user="$http_x_user_id" '
                         'x_numira_user="$http_x_numira_user_id" '
                         'x_forwarded_for="$http_x_forwarded_for"';
server {
    listen 80;
    server_name olega-mcp.ru;

    location /mcp {
        add_header WWW-Authenticate 'Bearer realm="mcp"' always;
        return 401;
        access_log /var/log/nginx/mcp_access.log debug_headers;

        proxy_pass http://mcp:8001/mcp;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header Authorization $http_authorization;
        proxy_set_header X-Forwarded-Proto http;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name olega-mcp.ru;

    ssl_certificate /etc/letsencrypt/live/olega-mcp.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/olega-mcp.ru/privkey.pem;

    location /exports/ {
        proxy_pass http://api:8000/exports/;
    }

    location /mcp {
        add_header WWW-Authenticate 'Bearer realm="mcp"' always;
        return 401;
        access_log /var/log/nginx/mcp_access.log debug_headers;

        proxy_pass http://mcp:8001/mcp;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header Authorization $http_authorization;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location / {
        proxy_pass http://api:8000/;
    }
}