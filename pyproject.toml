[project]
name = "slidelab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gradio[mcp]>=6.13.0",
    "pydantic-settings>=2.14.0",
    "requests>=2.33.1",
]

[tool.uv.workspace]
members = [
    "apps/mcp-ppt",
]
